import { Typography } from "@mui/material";
import { AppShell } from "@/layout/AppShell";

export function DashboardPage() {
  return (
    <AppShell>
      <Typography variant="body1" color="text.secondary">
        Filter bar and timeline chart land in the next phases.
      </Typography>
    </AppShell>
  );
}
